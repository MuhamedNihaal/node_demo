const num1 = 5;
const num2 = 10;

function addValue() {
    console.log(`The Sum is : ${num1 + num2}`);
}

addValue(); //here we dont need to export module because we already export it in the function thats how it works